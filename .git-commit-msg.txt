feat: resuelve los pendientes de la revisión (clubes ambiguos, verificación de email, términos, escudos, OG images, RSS, PWA, export CSV, reglas, monitoreo de errores, rate-limit en Postgres, índice de performance)

- **Clubes ambiguos (#2)**: /admin/clubes -panel para resolver a mano los
  42 clubes "combo" (varios clubes reales pegados con "/" o "|", 84
  regatistas) reasignando el club real de cada regatista individual, y
  la sigla "CNP" (dos nombres completos distintos y ambiguos) con un
  botón de fusión directa. Nuevos endpoints: GET /api/admin/clubes/combos,
  PATCH /api/admin/regatistas/[id]/club, POST /api/admin/clubes/merge.
- **Verificación de email (#3)**: el campo User.emailVerified existía
  pero no lo usaba nadie. Ahora /api/registro manda un mail de
  confirmación (mismo patrón de token que "olvidé mi contraseña", con
  un prefijo de identifier propio para no pisar el token del otro
  flujo) y /verificar-email lo consume.
- **Checkbox de términos (#4)**: registro ahora exige aceptar el Aviso
  Legal y la Política de Privacidad antes de crear la cuenta -se guarda
  el momento exacto en User.aceptoTerminosEn (no solo un booleano).
- **Escudos de club (#5)**: ClubAvatar -sin los logos reales de 180
  clubes, un color + iniciales determinísticos por nombre en vez del
  mismo ícono genérico en todos lados.
- **Open Graph dinámico (#6)**: opengraph-image.tsx por defecto y uno
  específico para campeonato/regatista/club (next/og, sin fuentes
  externas -Satori ya soporta los acentos del español, verificado).
- **RSS (#7)**: /feed.xml con los últimos 30 campeonatos publicados,
  linkeado desde el <head> y el footer.
- **PWA (#8)**: manifest.ts (permite "agregar a pantalla de inicio"),
  reusando los íconos que ya existían.
- **Export CSV (#9)**: CsvDownloadButton reutilizable -tabla de
  posiciones de un campeonato, historial de un regatista, ranking de un
  club.
- **Reglas de puntaje (#11)**: /reglas explica sistema de puntaje bajo,
  descartes, desempates, flotas y la fórmula del Puntaje FAY -linkeado
  desde el footer y desde la tabla de cada campeonato.
- **Monitoreo de errores (#12)**: sin cuenta de Sentry disponible,
  handleApiError ahora también persiste cada error en un ErrorLog
  propio (tabla nueva); /admin/errores lo lista con opción de ver el
  stack y borrar.
- **Rate-limit robusto (#13)**: reemplaza el Map en memoria (no
  compartido entre instancias serverless) por una tabla Postgres
  (RateLimitEntry) -mismos límites de antes, ahora consistentes entre
  instancias.
- **Performance de /clubes/[id] (#14)**: @@index([clubId]) en
  Regatista -la consulta de estadísticas de un club ya no escanea la
  tabla entera para encontrar quiénes son sus regatistas.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
